using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum MonsterClassType
{
    Basic,
    Elite
}

public enum MonsterType {

    Basic,
    Human,
    Flying,
    Destruct,
    Walk,
    Practice
}

public enum AttackType
{
    None,
    MeleeAttack,
    DestructAttack,
    RushAttack,
    StraightRangedAttack,
    ParabolaRangedAttack
}

public enum AttackPatternType
{
    First,
    Second,
    Third
}
